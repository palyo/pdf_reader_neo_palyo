<?xml version="1.0" encoding="utf-8"?>
<!--
  Outer height changed from the fixed `native_65dp` to `wrap_content` with a
  `minHeight` floor: the 3-line text stack (headline + Ad badge row + body)
  was being clipped at the bottom of the 65 dp container after the icon
  margins grew to `8sdp` for icon-column alignment. Now the row grows
  vertically when the text needs it, and stays at 65 dp on shorter content.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:minHeight="@dimen/native_65dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/native_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardBackgroundColor="@color/colorSecondaryBackground"
        app:cardCornerRadius="0dp"
        app:cardElevation="0dp"
        app:cardPreventCornerOverlap="false"
        app:cardUseCompatPadding="false">

        <com.google.android.gms.ads.nativead.NativeAdView
            android:id="@+id/unified"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!--
              Outer margins and icon dimensions deliberately match
              `layout_row_item_directory.xml` (file_card = 42sdp, 8sdp start /
              vertical margins; text_title starts 6sdp after the card). Without
              this, the ad's icon was 50dp at 5dp left margin while file PDF
              icons were 42sdp at 8sdp — making the icon column look jagged
              wherever the ad fell mid-list.
            -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginStart="@dimen/_8sdp"
                android:layout_marginTop="@dimen/_8sdp"
                android:layout_marginEnd="@dimen/_8sdp"
                android:layout_marginBottom="@dimen/_8sdp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!--
                  `fitCenter` preserves the icon's aspect ratio and never
                  crops — important because AdMob delivers asset icons in
                  arbitrary aspect ratios and `centerCrop` was chopping the
                  edges off non-square logos (e.g. the Google Ads test icon).
                -->
                <ImageView
                    android:id="@+id/ad_app_icon"
                    android:layout_width="@dimen/_42sdp"
                    android:layout_height="@dimen/_42sdp"
                    android:adjustViewBounds="true"
                    android:scaleType="fitCenter" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="@dimen/_6sdp"
                    android:orientation="horizontal">

                    <RelativeLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_vertical">

                        <TextView
                            android:id="@+id/ad_headline"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_toStartOf="@id/ad_call_to_action"
                            android:fontFamily="@font/bold"
                            android:maxLines="1"
                            android:minLines="1"
                            android:textColor="@color/colorText"
                            android:textSize="12sp" />

                        <LinearLayout
                            android:id="@+id/layout_details"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_below="@+id/ad_headline"
                            android:layout_marginTop="2dp"
                            android:layout_toStartOf="@id/ad_call_to_action"
                            android:gravity="center_vertical"
                            android:orientation="horizontal">

                            <TextView
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_gravity="center_vertical"
                                android:background="@drawable/ic_bg_rounded_4sdp"
                                android:backgroundTint="@color/colorAccent"
                                android:includeFontPadding="false"
                                android:paddingHorizontal="6dp"
                                android:paddingTop="2dp"
                                android:paddingBottom="2dp"
                                android:text="Ad"
                                android:textColor="#FFFFFF"
                                android:textSize="9dp" />

                            <TextView
                                android:id="@+id/ad_advertiser"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:gravity="bottom"
                                android:textColor="@color/colorText"
                                android:textSize="14sp"
                                android:textStyle="normal"
                                android:visibility="gone" />

                            <RatingBar
                                android:id="@+id/ad_stars"
                                style="?android:attr/ratingBarStyleSmall"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:isIndicator="true"
                                android:numStars="5"
                                android:progressBackgroundTint="@color/colorText"
                                android:progressTint="@color/colorAccent"
                                android:secondaryProgressTint="@color/colorAccent"
                                android:stepSize="0.5" />
                        </LinearLayout>

                        <TextView
                            android:id="@+id/ad_body"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_below="@+id/layout_details"
                            android:layout_toStartOf="@id/ad_call_to_action"
                            android:alpha="0.70"
                            android:maxLines="1"
                            android:minLines="1"
                            android:textColor="@color/colorTextOpacity"
                            android:textSize="9sp"
                            android:visibility="visible" />

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/ad_call_to_action"
                            android:layout_width="wrap_content"
                            android:layout_height="36dp"
                            android:layout_alignParentEnd="true"
                            android:layout_centerVertical="true"
                            android:layout_marginStart="12dp"
                            android:layout_marginTop="5dp"
                            android:backgroundTint="#0091EA"
                            android:gravity="center"
                            android:includeFontPadding="false"
                            android:insetLeft="0dp"
                            android:insetTop="0dp"
                            android:insetRight="0dp"
                            android:insetBottom="0dp"
                            android:paddingHorizontal="36dp"
                            android:text="Install"
                            android:textColor="@color/colorWhite"
                            android:textSize="14sp"
                            android:textStyle="bold"
                            app:cornerRadius="20dp" />
                    </RelativeLayout>
                </LinearLayout>
            </LinearLayout>
        </com.google.android.gms.ads.nativead.NativeAdView>
    </androidx.cardview.widget.CardView>
</RelativeLayout>